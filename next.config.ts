import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev overlay lands on the sidebar toggle and the first mobile tab.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
